import IdeaBank from '../../components/IdeaBank';

export const metadata = {
  title: 'IDEA_BANK | ClawdKitchen 🦀',
  description: '97 build ideas for the AI agent economy. Find your next hackathon project.',
};

export default function IdeaBankPage() {
  return <IdeaBank />;
}
